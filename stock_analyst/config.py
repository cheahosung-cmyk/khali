"""파이프라인 튜닝값 모음. 점수 가중치·필터 임계값·유니버스·모델 설정을 전부 여기서 조정한다."""

# ---------------------------------------------------------------------------
# Claude 리포트
# ---------------------------------------------------------------------------
CLAUDE_MODEL = "claude-sonnet-5"  # 비용을 더 줄이려면 "claude-haiku-4-5-20251001"
CLAUDE_MAX_TOKENS = 16000

# ---------------------------------------------------------------------------
# 공통 스코어링
# ---------------------------------------------------------------------------
# 가치 점수 V = 0.30*pctl(1/PER) + 0.25*pctl(1/PBR) + 0.25*pctl(ROE) + 0.20*pctl(DIV)
VALUE_WEIGHTS = {"per": 0.30, "pbr": 0.25, "roe": 0.25, "div": 0.20}
# 모멘텀 점수 M = 0.30*pctl(52주위치) + 0.30*pctl(MA20/MA60-1) + 0.25*pctl(60일수익률) + 0.15*pctl(거래량비)
MOMENTUM_WEIGHTS = {"range_pos": 0.30, "ma_ratio": 0.30, "ret60": 0.25, "vol_ratio": 0.15}
COMPOSITE_VALUE_WEIGHT = 0.6   # 종합 = 0.6*V + 0.4*M
RANGE_POS_CAP = 0.9            # 52주 위치 캡 (고점 추격 방지)
MIN_ROE_PCT = 5.0              # 밸류트랩 가드: ROE 5% 미만 탈락
MAX_PER = 40.0
MAX_PBR = 10.0
MIN_PRICE_ROWS = 130           # 모멘텀 계산에 필요한 최소 일봉 수 (MA60 + 60일 수익률)

TOP_N_PER_MARKET = 6           # 시장별 Claude에 넘기는 최종 후보 수

# ---------------------------------------------------------------------------
# 한국 (KOSPI/KOSDAQ) — pykrx 전 종목 스캔
# ---------------------------------------------------------------------------
KR_MARKETS = ["KOSPI", "KOSDAQ"]
KR_MIN_MARKET_CAP = 300_000_000_000   # 시총 3,000억 원 이상
KR_MIN_TRADING_VALUE = 1_000_000_000  # 당일 거래대금 10억 원 이상 (일평균의 근사)
KR_VALUE_PREFILTER_N = 60             # 시장별 가치 상위 N개만 개별 OHLCV 조회

# ---------------------------------------------------------------------------
# 미국 — S&P500 + NASDAQ-100 (대·중형주 한정, 리포트에 범위 명시)
# ---------------------------------------------------------------------------
US_MIN_MARKET_CAP = 2_000_000_000     # $2B 이상
US_MOMENTUM_PREFILTER_N = 120         # 모멘텀 상위 N개만 yfinance 펀더멘털 조회
US_DOWNLOAD_CHUNK = 100               # yf.download 청크 크기
US_STALE_DAYS = 4                     # 마지막 봉이 이보다 오래되면 데이터 신선도 경고

RETRY_ATTEMPTS = 3
RETRY_BACKOFF_SECONDS = 2             # 2, 4, 8초 지수 백오프
