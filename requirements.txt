# 코어(백테스트/페이퍼)는 표준 라이브러리만으로 동작합니다.
# 아래는 실거래 어댑터·운영 단계에서 필요합니다.

# KIS API 호출
aiohttp>=3.9.1
requests>=2.31.0

# 설정
python-dotenv>=1.0.0

# (선택) 분석·시각화
# pandas>=2.1.0
# matplotlib>=3.8.0

# 개발/테스트
pytest>=7.4.3
